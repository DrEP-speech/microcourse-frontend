export const metadata = { title: "Dashboard | MicroCourse" };

export default function DashboardPage() {
  return (
    <div style={{ maxWidth: 760, margin: "40px auto", padding: 24 }}>
      <h1>Dashboard</h1>
      <p>You're logged in. Replace this with charts, recent activity, etc.</p>
    </div>
  );
}